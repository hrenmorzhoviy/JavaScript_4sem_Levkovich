export class FormComponent { constructor(parent, title, fields, onSubmitCallback) { this.parent = parent; this.title = title; this.fields = fields; this.onSubmitCallback = onSubmitCallback; } render(initialData = {}) { const formDiv = document.createElement('div'); formDiv.className = 'modal-card'; formDiv.innerHTML = `<h3>${this.title}</h3><form id="dynamicForm">${this.fields.map(field => `<div style="margin-bottom:12px;"><label><strong>${field.label}:</strong></label><br><input type="${field.type || 'text'}" name="${field.name}" value="${initialData[field.name] || ''}" ${field.required ? 'required' : ''} style="width:100%;padding:6px;"></div>`).join('')}<button type="submit" style="background:#28a745;margin-top:8px;">Сохранить</button><button type="button" id="cancelFormBtn" style="background:#6c757d;">Отмена</button></form>`; this.parent.appendChild(formDiv); const form = formDiv.querySelector('#dynamicForm'); form.addEventListener('submit', (e) => { e.preventDefault(); const formData = new FormData(form); const data = {}; this.fields.forEach(field => { data[field.name] = formData.get(field.name); }); this.onSubmitCallback(data); }); formDiv.querySelector('#cancelFormBtn').addEventListener('click', () => formDiv.remove()); } }
